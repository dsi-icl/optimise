import CreatePatientHandle, { SelectField as SelectFieldHandle } from './createPatient';

export default CreatePatientHandle;
export const CreatePatient = CreatePatientHandle;
export const SelectField = SelectFieldHandle;
