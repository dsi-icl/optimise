{
    "name": "optimise",
    "version": "1.10.4",
    "description": "Optimise:MS",
    "keywords": [
        "ms"
    ],
    "author": {
        "email": "f.guitton@imperial.ac.uk",
        "name": "Florian Guitton"
    },
    "repository": "github:dsi-icl/optimise",
    "homepage": "https://github.com/dsi-icl/optimise",
    "license": "MIT",
    "private": true,
    "workspaces": {
        "packages": [
            "packages/*"
        ],
        "nohoist": [
            "**/electron-builder",
            "**/electron-builder/**",
            "**/electron-webpack",
            "**/electron-webpack/**"
        ]
    },
    "engines": {
        "yarn": "^1.10.0"
    },
    "scripts": {
        "build": "lerna run build --scope optimise-core --scope optimise-docker --scope optimise-electron",
        "preelectron": "yarn build",
        "electron": "lerna run electron --scope optimise-electron",
        "lint": "echo Put your favorite linter here.",
        "prepackage": "yarn build",
        "package": "lerna run package --scope optimise-docker --scope optimise-electron --stream",
        "publish": "lerna run package --scope optimise-docker --scope optimise-electron --stream",
        "start": "lerna exec --parallel --stream --scope optimise-core --scope optimise-ui yarn start",
        "test": "lerna run test --parallel"
    },
    "devDependencies": {
        "cross-env": "5.2.0",
        "lerna": "3.13.3"
    }
}